---
description: Minecraft 插件開發 Agent Skills 專案規劃與實作規格指引
alwaysApply: true
---

# Minecraft 插件開發 Agent Skills 專案規格

你是一位軟體架構師與產品設計助理。

**專案目標**：為 Minecraft Java Edition 插件（Bukkit/Spigot/Paper API）建立可復用的 Agent Skills 函式庫，協助開發者自動化常見任務。

---

## Skills 存放規範

**所有 Skill 統一存放於** `.cursor/skills/<skill-name>/` 目錄下。

```
.cursor/
├── rules/
│   └── minecraft-plugin-agent-skills.mdc   ← 本規格文件
└── skills/
    ├── skills-registry.yml                  ← Skill 總覽索引
    ├── spigot-paper-api-caller/             ← 已建立
    │   ├── SKILL.md
    │   └── api-reference.md
    ├── generate-plugin-skeleton/            ← 待建立
    ├── generate-config-yml/
    ├── generate-command-handler/
    ├── generate-event-listener/
    ├── generate-test-suite/
    ├── generate-cicd-workflow/
    ├── generate-database-manager/
    └── generate-placeholder-expansion/
```

---

## 新增 Skill 步驟

1. 在 `.cursor/skills/` 下建立新目錄（slug 命名，小寫加連字號）
2. 建立 `SKILL.md`（含 YAML frontmatter：`name`、`description`）
3. 如有詳細參考資料，建立 `reference.md` 或 `examples.md`
4. 在 `.cursor/skills/skills-registry.yml` 新增條目
5. 送出 PR，CI 自動驗證

---

## Skill 規格範本

每個 `SKILL.md` 至少涵蓋：

```
技能名稱：[英文 slug]
目的（1 行）：[核心功能說明]
輸入範例：{"pluginName": "MyPlugin", "mcVersion": "1.20.4"}
輸出範例：[檔案清單或代碼片段]
觸發條件：[使用者說出哪些關鍵字時啟動]
失敗回退：[出錯時的處理方式]
範例 prompt：[可直接放入 agent 的 prompt]
```

---

## 已規劃的 Skill 清單（8 個）

| ID | Skill 名稱 | 目的 | 狀態 |
|----|-----------|------|------|
| 01 | `spigot-paper-api-caller` | 產生正確的 Spigot/Paper Java API 調用代碼 | ✅ 已建立 |
| 02 | `generate-plugin-skeleton` | 產生完整 Maven 插件骨架 | 待建立 |
| 03 | `generate-config-yml` | 產生結構化 config.yml | 待建立 |
| 04 | `generate-command-handler` | 產生 Command + TabCompleter 類 | 待建立 |
| 05 | `generate-event-listener` | 產生 Event Listener 骨架 | 待建立 |
| 06 | `generate-test-suite` | 產生 JUnit5 + MockBukkit 測試套件 | 待建立 |
| 07 | `generate-cicd-workflow` | 產生 GitHub Actions CI/CD | 待建立 |
| 08 | `generate-database-manager` | 產生 SQLite/MySQL DatabaseManager | 待建立 |
| 09 | `generate-placeholder-expansion` | 產生 PlaceholderAPI Expansion | 待建立 |

---

## 執行緒安全原則（所有 Skill 需遵守）

- **主執行緒**：所有 Bukkit 物件操作（Player、World、ItemStack）
- **非同步**：IO、資料庫、HTTP 請求
- 從非同步切回主執行緒：`Bukkit.getScheduler().runTask(plugin, () -> {})`

---

## 開發里程碑

| 里程碑 | 內容 | 時程 |
|--------|------|------|
| M1 MVP | Skill 01–04 完成，Registry 建立 | 第 1–2 週 |
| M2 擴充 | Skill 05–07，單元測試自動化 | 第 3–4 週 |
| M3 完整 | Skill 08–09，整合測試（javac 驗證） | 第 5–6 週 |
| M4 維護 | Folia 支援、Skill 市集、多語言 prompt | 第 7 週起 |

---

## 風險緩解

1. **MC API 版本碎片化** → Skill 輸入含 `mcVersion`，維護版本對應表
2. **LLM 輸出不穩定** → 整合 `javac` 語法驗證，失敗自動重試
3. **Prompt Injection** → 輸入白名單 + schema 嚴格驗證
4. **技術債** → 每 Skill 獨立目錄、強制規格文件、PR review
5. **依賴過時** → CI/CD 定期執行 `mvn versions:display-dependency-updates`
