---
description: MPS 專案規格：Minecraft Plugin Studio Agent Skills 函式庫操作規範（Paper / Purpur / Velocity / Waterfall）
alwaysApply: true
---

# Minecraft Plugin Studio (MPS) — Agent Skills 操作規範

**專案目標**：為 Minecraft Java Edition 插件開發提供可復用的 Agent Skills 函式庫，覆蓋 Paper、Purpur、Velocity、Waterfall 四個平台，協助開發者自動化常見任務。

---

## 目錄結構

```
.cursor/skills/<skill-slug>/                  ← Paper 舊版技能（flat 路徑）
.cursor/skills/<platform>/<skill-slug>/       ← 新平台技能（巢狀路徑）
Skills/<skill-slug>/                          ← 對外可讀鏡像（flat，Paper 舊版）
Skills/<platform>/<skill-slug>/               ← 對外可讀鏡像（巢狀，新平台）
Skills/<platform>/PLATFORM.md                 ← 平台概覽（build.gradle、主類模板、API 差異）
Skills/_shared/                               ← 跨平台共用指南
```

兩個路徑（`.cursor/skills/` 和 `Skills/`）**必須保持同步**，任何新增或修改都要同時更新。

---

## 已完成 Skills（19 個，全部 active）

### Paper / Spigot Skills（flat 路徑）

| Slug | 類別 | 功能 |
|------|------|------|
| `spigot-paper-api-caller` | integration | Bukkit/Paper Java API 調用代碼 |
| `generate-plugin-skeleton` | scaffold | Maven 插件完整骨架 |
| `generate-config-yml` | config | config.yml + ConfigManager |
| `generate-command-handler` | command | CommandExecutor + TabCompleter |
| `generate-event-listener` | event | Event Listener 骨架 |
| `generate-test-suite` | testing | JUnit 5 + MockBukkit 測試套件 |
| `generate-cicd-workflow` | devops | GitHub Actions CI/CD workflow |
| `generate-database-manager` | database | SQLite/MySQL + HikariCP |
| `generate-placeholder-expansion` | integration | PlaceholderAPI Expansion |
| `generate-permission-system` | permission | PermissionManager + plugin.yml 宣告 |
| `generate-message-system` | config | messages.yml + MessageManager |
| `integrate-vault` | integration | Vault 經濟 API 整合 |
| `sync-website-skill` | devops | 同步 Skill 到 MPS 網站（Cursor-only） |

### 新平台 Skills（巢狀路徑 `<platform>/<slug>`）

| Slug | 平台 | 類別 | 功能 |
|------|------|------|------|
| `purpur/purpur-api-caller` | purpur | integration | Purpur-specific API 代碼（PlayerAFKEvent 等） |
| `velocity/generate-velocity-plugin-skeleton` | velocity | scaffold | Velocity proxy plugin Gradle 骨架 |
| `velocity/generate-proxy-event-listener` | velocity | event | @Subscribe 事件監聽器 |
| `velocity/generate-plugin-message-handler` | velocity | integration | PluginMessageEvent 處理器 |
| `waterfall/generate-waterfall-plugin-skeleton` | waterfall | scaffold | Waterfall/BungeeCord proxy plugin 骨架 |
| `waterfall/generate-bungeecord-channel` | waterfall | integration | BungeeCord channel 處理器 |

詳細索引見 `.cursor/skills/skills-registry.yml`。

---

## 新增 Skill 步驟

### Paper 技能（flat 路徑，向後相容）
1. 在 `.cursor/skills/<slug>/` 建立目錄
2. 同步在 `Skills/<slug>/` 建立相同目錄
3. 建立 `SKILL.md`（含 YAML frontmatter：`name`、`description`）
4. 建立 `examples.md`（至少 2 個範例）
5. 在兩個 `skills-registry.yml` 新增條目（`status: active`）

### 平台技能（巢狀路徑）
1. 在 `.cursor/skills/<platform>/<slug>/` 建立目錄
2. 同步在 `Skills/<platform>/<slug>/` 建立相同目錄
3. 建立 `SKILL.md`（含 `name`、`description`，加入 `platform` 欄位）
4. 建立 `examples.md`（至少 2 個範例）
5. 在兩個 `skills-registry.yml` 新增條目，`skill_file` 使用巢狀路徑
6. 更新本 `.mdc` 文件的技能列表與數量

---

## SKILL.md 必填欄位

```
技能名稱 / Skill Name：[英文 slug]
目的（1 行）/ Purpose：[核心功能說明 — 中英雙語]
觸發條件 / Triggers：[使用者說出哪些關鍵字時啟動 — 中英皆需]
輸入參數 / Inputs：[需要使用者提供的資訊]
輸出產物 / Outputs：[產生的檔案或代碼清單]
代碼範本 / Code Template：[完整且有標注的代碼範例]
失敗回退 / Fallback：[出錯時的處理方式]
```

---

## 執行緒安全原則（所有 Skill 產生的代碼需遵守）

### Paper / Bukkit
- **主執行緒**：所有 Bukkit 物件操作（Player、World、ItemStack）
- **非同步**：IO、資料庫、HTTP 請求
- 從非同步切回主執行緒：`Bukkit.getScheduler().runTask(plugin, runnable)`

### Velocity
- 所有 `@Subscribe` handler 都在 async thread pool 執行
- 不需要切換回「主執行緒」——直接使用 Velocity API
- 不可使用 `Thread.sleep()` 或 blocking IO——改用 `proxyServer.getScheduler().buildTask()`

### Waterfall / BungeeCord
- 大多數操作在 main proxy thread 執行（類似 Bukkit）
- 使用 `getProxy().getScheduler().runAsync(plugin, runnable)` 執行非同步工作

---

## 平台技能選擇指引

- 使用者問「後端插件」、「伺服器插件」→ **Paper** 或 **Purpur** skills
- 使用者問「代理插件」、「proxy plugin」、「hub」 → **Velocity** 或 **Waterfall** skills
- 使用者問「跨伺服器通訊」 → 先確認 proxy 類型，再選對應的 messaging skill
- Purpur = Paper API + 小量擴充，先嘗試 Paper skill
- Velocity 和 Waterfall 的 plugin messaging API 完全不同，勿混用
